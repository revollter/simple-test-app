{{ form_start(form, { attr: { id: 'data-form' }, action: path('data_save'), method: 'POST' }) }}
{{ form_row(form.product, { attr: { 'data-add-data-target': 'product' } }) }}
{{ form_row(form.color, { attr: { 'data-add-data-target': 'color' } }) }}
{{ form_row(form.amount) }}

<div class="mt-3">
    <div class="row">
        <div class="col">
            <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
            <button class="btn btn-primary float-end" id="submit-data">Save</button>
        </div>
    </div>
</div>
{{ form_end(form) }}